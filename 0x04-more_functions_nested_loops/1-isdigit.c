#include "main.h"

/**
 * _isdigit - check if a character is a digit
 * @c : character to check
 * Return: int
 */

int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
	return (1);
}
else
{
	return (0);
}


}
